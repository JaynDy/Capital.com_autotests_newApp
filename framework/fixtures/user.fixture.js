export const user = {
  unregistered: {},

  registered: {
    email: process.env.TEST_EMAIL,
    password: process.env.TEST_PASSWORD,
  },

  authorised: {
    email: process.env.TEST_EMAIL,
    password: process.env.TEST_PASSWORD,
  },
};
